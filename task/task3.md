for the phoneme segmenter, current method can not process complex word once it complex.

Now, use open_ai api to do this task, input word, and return phoneme segmenter
then, generate audio for the phoneme as current pipline